using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemBehaviourManager : MonoBehaviour
{
	/*int index = -1 ;
	string itemName = "" ;
	string address = "" ;

	public void InitValues(int _index, string _name, string _address)
	{
		index = _index ;
		itemName = _name ;
		address = _address ;

		GetComponent<ButtonManager>().buttonText = name + " / " + address ;
	}

	public void ChooseItem()
	{
		Debug.Log("Index : " + index) ;
		Debug.Log("Name : " + name) ;
		Debug.Log("Address : " + address) ;
		BleManagement bleManagement = GameObject.Find("SessionManager").GetComponent<BleManagement>() ;
		bleManagement.MAC_ADDRESS = address ;
		bleManagement.InitHRM() ;
		PlayerPrefs.SetString("lastDeviceName", name);
		PlayerPrefs.SetString("lastDeviceAddress", address);

		GameObject.Find("/Canvas").SetActive(false) ;
	}*/
}
