using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InvertMaterial : MonoBehaviour
{

    public Material m_Emissive;
    public Material m_Color;


    // Start is called before the first frame update
    void Start()
    {

        /*gameObject.transform.GetComponent<MeshRenderer>().materials[0] = m_Emissive;
        gameObject.transform.GetComponent<MeshRenderer>().materials[1] = m_Emissive;*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
